name: Keep Streamlit Awake

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Streamlit app
        run: curl -c cookies.txt -b cookies.txt -sL https://my-qr-app.streamlit.app/ > /dev/null
